document.getElementByName('button').addEventListener(click,comparePassword);
// fonction qui change la couleur des bordures du form en vert ou rouge selon si les password sont exactement les mêmes
function comparePassword(){
  if (document.getElementById('password') == document.getElementById('confirmPassword')) {
    
  }
}
